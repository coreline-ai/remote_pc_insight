# CHANGELOG.md — Changelog

## [0.1.1] - Deployment target update
- Remove legacy deployment artifacts
- Add Vercel API deployment files (`server/vercel.json`, `server/api/index.py`, `server/requirements.txt`)
- Update production env examples for Vercel + Neon
- Add Vercel + Neon deployment checklist

## [0.1.0] - MVP
- Device enrollment (enroll token -> device token)
- Remote command queue (polling)
- Report upload + web viewer
- Outbox retry + processed store
- Basic dashboard pages

## [0.2.0] - v1 planned
- Web auth (JWT/OAuth)
- Device revoke UI
- Device settings (upload level)
- Server-side sanitize enforcement
- Agent auto-start installers

---
